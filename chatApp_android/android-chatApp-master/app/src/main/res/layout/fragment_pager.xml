<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="0dip"
    android:background="@android:color/black" >

    <EditText
        android:id="@+id/etAppId_FragmentPager"
        android:layout_marginTop="10dp"
        android:background="@drawable/custombutton"
        android:layout_width="235dp"
        android:layout_height="wrap_content"
        android:hint="Launch App Before Share"
        />
    <Button
        android:id="@+id/btnLaunch_FragmentPager"
        android:layout_toRightOf="@+id/etAppId_FragmentPager"
        android:layout_marginTop="10dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/custombutton"
        android:textSize="20sp"
        android:textStyle="bold"
        android:textColor="#000000"
        android:onClick="onClick"
        android:text="Launch App"/>

    <android.support.v4.view.ViewPager

        xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/pager"
        android:layout_below="@+id/etAppId_FragmentPager"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent" />
</RelativeLayout>
